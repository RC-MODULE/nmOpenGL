#ifndef LINE_H
#define LINE_H

struct Point {
    // Point();
    // Point(int newX, int newY);
    // Point& operator=(Point& point);
    int x;
    int y;
};

// struct Line {
//     Line();
//     Line(Point& newPoint0, Point& newPoint1);
//
//     Point point0;
//     Point point1;
// };

#endif
